# Used by `docker compose up` and by `go run ./cmd/web`.
# When the Go app runs on your Mac and MySQL runs in Docker, use port 3309.
DB_USER=web
DB_PASSWORD=pass
DB_NAME=snippetbox
DB_HOST=127.0.0.1
DB_PORT=3309
ADDR=:4000
